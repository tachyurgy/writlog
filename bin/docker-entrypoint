#!/bin/bash -e
# Migrations (not db:prepare/schema load) so the append-only trigger is created
# by the same code in every environment without needing psql in the image.
if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
  ./bin/rails db:migrate
fi
exec "${@}"
