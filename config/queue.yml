default: &default
  dispatchers:
    - polling_interval: 1
      batch_size: 100
  workers:
    - queues: "*"
      threads: 2
      polling_interval: 0.5

development:
  <<: *default

production:
  <<: *default
